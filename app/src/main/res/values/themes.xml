<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimplyStockProj" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SimplyStockProj" parent="Base.Theme.SimplyStockProj" />

    <style name="Theme.SimplyStockProj.withActionBar" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">@color/SimplyStockBlue</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>